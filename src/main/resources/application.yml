
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Scolaris_CMdb
    username: postgres
    password: Jupitere10
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update          # 'create' pour le premier lancement, puis 'update'
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # D?sactiver l'auto-configuration OAuth2 inutile (on g?re le JWT manuellement)
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:           # laisser vide pour JWT auto-sign?

server:
  port: 8081

# Configuration JWT personnalis?e
application:
  jwt:
    secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970  # cl? HS256 en hex (? changer en prod!)
    access-token-expiration: 900000          # 15 minutes en ms
    refresh-token-expiration: 604800000      # 7 jours en ms

logging:
  level:
    com.rh: DEBUG
    org.springframework.security: INFO

